{
  "private": true,
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "scripts": {
    "prettier": "prettier --write {./,src/**/}*.{js,json}",
    "watch": "gulp watch",
    "build": "gulp"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-terser": "^1.2.0",
    "prettier": "^1.19.1"
  }
}
